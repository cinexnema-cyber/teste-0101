// Test user creation moved to Supabase-only implementation
// This file is no longer needed as users are created through Supabase auth routes

export const createTestUser = async () => {
  console.log("User creation now handled by Supabase auth routes");
};
