// Admin initialization moved to Supabase-only implementation
// Admin users are now created through Supabase auth routes

export const initAdmin = async () => {
  console.log("Admin initialization now handled by Supabase auth routes");
};
