// MongoDB Subscription model removed - using Supabase for subscription management
// All subscription operations are now handled through Supabase tables

export const Subscription = null;
export default null;
